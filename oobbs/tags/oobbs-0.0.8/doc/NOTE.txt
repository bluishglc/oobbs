对象间的直接依赖关系以字段形式表示是必要而合理的。如果我们需要通过对象导航特别是经过多级一对多导航来查找符合某些条件的对象时，直接以对象图导航的方式查找显然是低效的。这时候就是需要在O和R之间进行平衡的时候。面对这种情况，我们或许通过service的方式直接返回经sql查询到的结果会更好些。或者是通过hibernate的抓取计划加载对象。总之所加载的对象是通过其他方式筛选而加载出的，并非通过对象导航得到的。